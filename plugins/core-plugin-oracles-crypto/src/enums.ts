export enum OracleTransactionType {
    OracleRequest = 0,
    OracleResult = 1,
}

export const OracleTransactionGroup = 3;

export enum OracleTransactionStaticFees {
    OracleRequest = "1000000000",
    OracleResult = "0",
}
